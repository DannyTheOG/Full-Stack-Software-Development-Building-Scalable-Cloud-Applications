/**
    navigator object is a property of the window object that contains information about the browser. 
    it has properties like 'geolocation', 'cookieEnabled'.
*/
console.log( navigator );


/*
    userAgent property is a unique string which identifies any browser and it's version.
*/
console.log( navigator.userAgent )